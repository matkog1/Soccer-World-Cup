<Window x:Class="YourNamespace.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400">
    <Grid>
        <!-- Define grid rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Define grid columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Place controls in grid cells -->
        <ComboBox Name="ResolutionComboBox" Grid.Row="0" Grid.Column="1"/>
        <ComboBox Name="LanguageComboBox" Grid.Row="1" Grid.Column="1"/>
        <ComboBox Name="ChampionshipComboBox" Grid.Row="2" Grid.Column="1"/>
        <Button Content="Login" Grid.Row="3" Grid.Column="1" Click="LoginButton_Click"/>
    </Grid>
</Window>
